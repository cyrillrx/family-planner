# Kotlin Multiplatform & Compose Multiplatform Conventions

> [!IMPORTANT]
> **Canonical source of truth** (shared, project-agnostic): [`conventions/kmp-conventions.md`](https://github.com/cyrillrx/coding-conventions/blob/main/conventions/kmp-conventions.md) — do not duplicate here.

The full KMP / Compose Multiplatform conventions (MVVM + UDF, state & event modeling, navigation,
lifecycle-aware refresh, Compose rules, naming, formatting, testing) live in the canonical document
and apply as-is to the `cmp-app/` client.

## Project-specific additions

Nothing yet. `cmp-app/` is not initialized, so this project has no concrete bindings to document.

The PR that initializes the KMP project adds them here or in [`AGENTS.md`](../../AGENTS.md):
module split, package layout, dependency injection, navigation pattern, design system tokens, test
location and coverage policy.
